# 蚊媒监测数据处理程序 —— 最终需求文档（v3.5）

> v3.5 变更：GUI 新增“广州市表文件（可选）”输入口，总库输入改称“输入总库表文件”；总库中广州市缺失“地市-区/县/市-街道/乡/镇”且与广州表其余重合列完全一致时按广州表补充，以补充后的总库为基础文件（D41）；风险水平背景色改为参照一览表示例底纹：安全 92D050 / 低风险 FFFF00 / 中风险 FFC000 / 高风险 FF0000（D42）。
> v3.4 变更：取消“一览表 Word”输出（3 个输出）；村居一览表 Excel 改名并新增「BI+ADI整合表」sheet；所有文件字体 中文仿宋_GB2312/英文Times New Roman；村居一览表字号 整合表五号、其余14；风险水平背景色用 Excel 标准色；删除说明新增“监测日期”列并按删除原因排序；计算过程「BI+SSI(重复)+取较大值处理」sheet 背景无色（D35–D40）。
> v3.3 变更（按金标准「BI-报出整理」「ADI-报出整理」校准）：数值保留源精度不取整（P4/3.5）；SSI 仅当无同键 BI 时 ×2 并入，有同键 BI 时丢弃（B5/B6）；区县正则支持 `市` 后缀（阳春市/普宁市/罗定市/四会市/台山市）；ADI 指标列名改为 `ADI`；地址区分仅对组内 ≥2 个不同地址的行改名（D27–D31）。
> v3.2 变更：新增第 4 个输出“一览表 Word（BI+ADI 整合表）”，按示例文档「全省媒介伊蚊传染病疫点重点镇（街道）蚊媒密度监测村居一览表（8月20日20：00）.docx」实测排版（§3.7.1）；一览表 Excel 与一览表 Word 命名统一为月日不补零（D23–D26）。
> v3.1 变更：按示例文档「全省媒介伊蚊传染病疫情蚊媒监测情况（8月14日20：00）.docx」实测，新增 §3.6.1 日报 Word 排版规范；大标题/副题行/章节编号/页边距/字体字号均按示例文档；日报文件名按示例（全省前缀、月日不补零）；监测点数口径修正为“不重复监测地点数”（D19–D22）。
> 本版为最终实施依据：已按推荐决策（D1–D31，见 §8 决策记录）全部落实，无待确认项。
> 需求中的格式、规则、命名均以本版为准。

---

## 1. 总体目标

开发一个 Windows 可执行程序（.exe），具备图形用户界面（GUI）。用户通过界面选择 Excel 源文件、输入目标日期（年月日）及排除字段（可选），程序自动处理数据，生成三份文件：

1. 一份日报 Word（.docx）——叙述版（一、BI 调查评估；二、ADI 快速评估，见 3.6）
2. 一份村居一览表 Excel（.xlsx）——Sheet1 BI表 / Sheet2 ADI表 / Sheet3 BI+ADI整合表 / Sheet4 删除数据情况说明（见 3.7）
3. 一份计算过程 Excel（.xlsx）——9 个 Sheet（见 3.10）

程序兼容 Win7 及以上主流 Windows 系统，使用 Python 开发，GitHub Actions 打包为 exe。

## 2. 输入数据约定

### 2.1 必需列（10 列，列名完全匹配，顺序不限）

| 列名 | 说明 | 可否为空 |
|---|---|---|
| 监测时间（年/月/日） | 日期（实测为 datetime 类型） | 否（为空则删除） |
| 监测指标值 | 数值 | 否（为空则删除） |
| 地市-区/县/市-街道/乡/镇 | 文本（实测无分隔符，见 2.2） | 否 |
| 距末例天数（自动计算） | 数值（实测范围 -2 ~ 46263） | 否 |
| 防控区类型 | 文本（实测 5 种，见 2.2） | 否 |
| 社区/村居 | 文本 | 否 |
| 监测方法（如BI/RI/MOI/ADI等） | 文本（实测 9 种，见 2.2） | 否 |
| 监测地址（地图定位版） | 文本 | 可空 |
| 监测地址（如“监测地址”定位字段不可用，可手填；如定位可用，不需要重复填写） | 文本 | 可空 |

### 2.2 真实数据格式说明（实测）

- `地市-区/县/市-街道/乡/镇` 实际为“广东省+地市+区县+街道”**直接拼接、无任何分隔符**，格式：
  `广东省{地市}市{区县}{区/县/市}{街道}{街道/镇/乡}`
  例：`广东省广州市白云区人和镇`；`广东省东莞市市辖区黄江镇`；`广东省肇庆市端州区端州区黄岗街道`（端州区在街道部分重复出现）。
- 监测方法共 9 种：`布雷图指数BI`、`标准间指数SSI`、`成蚊密度指数法ADI`、`诱蚊诱卵器指数MOI`、`路径指数RI`、`BGs-Trap/化学信息素诱捕法`、`双层叠帐法`、`诱蚊灯法`、`人诱`。
- 防控区类型共 5 种：`核心区`、`警戒区`、`监控区`、`重点场所`、`核心区和警戒区综合`。
- 监测时间为 datetime（含 00:00:00 时间部分），日期比较只取 date 部分。

### 2.4 广州市表补全（可选，D41）

- GUI 提供“广州市表文件（可选）”输入口（总库输入改称“输入总库表文件”）。
- 总库中 `地市 = 广州市` 且 `地市-区/县/市-街道/乡/镇` 缺失的行，若与广州表在**其余全部重合列**上完全一致（数值相等视为一致；唯一匹配），则用广州表的该列值补充。
- 无法唯一匹配的行保持缺失，后续按“行政区划无法解析”处理并记入删除说明。
- 以补充后的总库作为后续处理的基础文件（实测 0820 样例：9801 行缺失，成功补充 9559 行）。

### 2.5 列校验与多余列处理

- 校验：上述 10 列必须**全部存在且列名完全匹配**；否则弹窗列出缺失列名并友好退出。
- 源文件其余 22 列（含 `地市-区/县/市-街道/乡/镇2`、`环境类型`、`备注` 等）一律忽略，不参与处理、不参与输出（Sheet3 删除说明除外，见 3.9）。

## 3. 数据处理流程

### 3.1 全局预处理（按顺序执行）

| 步骤 | 操作 | 删除原因（供 Sheet3） |
|---|---|---|
| P1 | 筛选 监测时间(date 部分) == 目标日期 的记录 | 不列入 Sheet3 |
| P2 | 删除监测时间为空的记录 | 监测时间为空 |
| P3 | 删除监测指标值为空的记录 | 监测指标值为空 |
| P4 | 监测指标值**保留源数据原精度，不取整**（金标准校准，见 3.5） | — |
| P5 | 若用户输入排除字段：删除 `地市-区/县/市-街道/乡/镇` 字符串包含该字段的记录（子串匹配；字段与单元格均 strip 后再匹配。例：输入“荔湾区”删除所有含“荔湾区”的行） | 被排除字段过滤 |
| P6 | 保留 距末例天数 ≤ 5 或 > 40000 的记录（负值、0 均视为 ≤5 保留） | 距末例天数不在范围内 |
| P7 | 仅保留 防控区类型 ∈ {核心区, 警戒区}（“监控区”“重点场所”“核心区和警戒区综合”一律删除） | 防控区类型不符合 |
| P8 | 新增列 监测地点 = `{社区/村居}（{防控区类型}）` | — |
| P9 | 新增列 地市、区县、街道（解析规则见 3.2） | 行政区划无法解析 |
| P10 | 预处理完成 → 得到 **基础数据集** | — |

### 3.2 地市 / 区县 / 街道 解析规则（真实格式）

输入串 S = `广东省{地市}市{区县}{区/县/市}{街道}{街道/镇/乡}`：

1. S 为空或非空但不以 `广东省` 开头 → 无法解析，该行删除，原因“行政区划无法解析”。
2. 去掉前缀 `广东省`。
3. 按固定 21 地市顺序（广州、深圳、珠海、汕头、佛山、韶关、河源、梅州、惠州、汕尾、东莞、中山、江门、阳江、湛江、茂名、肇庆、清远、潮州、揭阳、云浮）逐一尝试：若剩余部分以 `{地市名}市` 开头 → **地市 = 该地市名**（不含“市”），并去掉 `{地市名}市`。
4. 区县：从剩余部分开头用正则 `^(市辖区|.+?区|.+?县|.+?市)` 匹配取前缀（例：`白云区人和镇`→`白云区`；`封开县平凤镇`→`封开县`；`市辖区黄江镇`→`市辖区`；`阳春市春湾镇`→`阳春市`）。
5. 街道 = 剩余部分（含 街道/镇/乡 后缀，原样保留）。
6. **端州区特例（一般化规则）**：若街道以区县名开头，去掉该前缀。例：`端州区黄岗街道` → `黄岗街道`。
7. 三种输出口径：
   - **内部字段**（计算/排序用）：地市不带“市”（`广州`）；区县原样（`天河区`、`市辖区`）；街道原样。
   - **监测点汇总表显示**：地市同内部；区县去掉末尾“市/县/区”字（`天河区`→`天河`、`封开县`→`封开`），`市辖区`→`/`；街道原样。
   - **Word 风险列表显示**：地市 = 内部地市 + `市`（`广州市`）；区县原样（`天河区`、`封开县`），`市辖区` 时整个区县段位删除（“东莞市市辖区” → “东莞市”）；街道原样。

### 3.3 BI + SSI 专项处理

**B1** 从基础数据集筛选 监测方法 ∈ {`布雷图指数BI`, `标准间指数SSI`} → **BI 数据处理表**（内存中间表，不输出为 Sheet）。其余方法（MOI、RI、BGs-Trap、双层叠帐法、诱蚊灯法、人诱）的行记入 Sheet3，原因“监测方法不属于BI/SSI/ADI（不参与计算）”。

**B2** SSI 转换：SSI 行 监测指标值 × 2，得“转换后SSI值”（**保留原精度，不取整**）。

**B3** Sheet1「SSI表」：全部 SSI 行（转换后值）。列 = 地市、区县、街道、地市-区/县/市-街道/乡/镇、社区/村居、监测地址（地图定位版）、监测地址（手填）、防控区类型、监测地点、监测方法、监测指标值（转换后）。

**B4** 判定“一致”的键值 K = (地市-区/县/市-街道/乡/镇, 社区/村居, 监测地址（地图定位版）, 监测地址（手填）, 防控区类型)。**空值一律视为空字符串**参与比较。

**B5** SSI 归属（按金标准「BI-报出整理」校准，替代原 max 合并）：
- SSI 行若 K **不在**任何 BI 行的 K 中（无同键 BI）→ 监测方法改为 `布雷图指数BI`、指标值用转换后 SSI 值，**并入 BI 数据集**。
- SSI 行若 K 与某条 BI 行一致 → **该 SSI 行丢弃，不并入、不取大**（保留 BI 行）。
→ Sheet2「BI+SSI(不重复)」：内容 = 原始 BI 行 + 并入的 SSI 行；**来自 SSI 的行背景填充黄色**。

**B6** 同键 SSI 追溯：被丢弃的同键 SSI 行集中展示。
→ Sheet3「BI+SSI(重复)+取较大值处理」：内容 = 被丢弃的同键 SSI 行（**背景标红**），列 = Sheet1 列 + 新增三列 `原BI值`、`原SSI值`、`转换后的SSI值`（`原BI值` = 同键 BI 行最大值）。

**B7** 完整键去重：对 BI 行与并入的 SSI 行分别按 K 分组，**每组只保留指标值最大的行**，其余删除。
→ Sheet4「重复数据删除(BI)」：内容 = **全部行**（保留行 + 被删行），被删除行（每组非最大值行）**背景标红**。

**B8** 地址区分：对 B7 结果中 `(地市-区/县/市-街道/乡/镇, 社区/村居, 防控区类型)` 组内**存在 ≥2 个不同地址**的行，修改 监测地点 = `{社区/村居}{最小区分地址}（{防控区类型}）`（算法见 3.8）。
→ Sheet5「地址区分处理(BI)」：内容 = 全部行，**被修改行背景标黄**（不新增备注列）。

**B9** 最终 BI 表 = B8 结果。列 = 地市、区县、街道、监测地点、BI*、风险水平*（风险分级见 3.5）。
→ Sheet6「最终表(BI)」。地市/区县/街道列即按 3.2 解析结果，监测点表直接复用，不再二次解析。

### 3.4 ADI 专项处理

**A1** 从基础数据集筛选 监测方法 == `成蚊密度指数法ADI` → **ADI 数据处理表**（内存中间表）。

**A2** 完整键去重（K 同 B4）：每组保留指标值最大的行，其余删除。
→ Sheet1「重复数据删除(ADI)」：内容 = 全部行，被删行背景标红。

**A3** 地址区分（同 3.8 算法）。
→ Sheet2「地址区分处理(ADI)」：内容 = 全部行，被修改行背景标黄。

**A4** 最终 ADI 表。列 = 地市、区县、街道、监测地点、ADI、风险水平*（ADI 分级；**指标列名用 `ADI`**，与金标准「ADI-报出整理」及一览表一致）。
→ Sheet3「最终表(ADI)」。

### 3.5 风险分级表（BI 与 ADI 各用各的分级）

| 方法 | 安全 | 低风险 | 中风险 | 高风险 | 背景色 |
|---|---|---|---|---|---|
| BI（布雷图指数） | x < 5 | 5 ≤ x < 10 | 10 ≤ x < 20 | x ≥ 20 | 绿/黄/橙/红 |
| ADI（成蚊密度） | x ≤ 2 | 2 < x ≤ 5 | 5 < x ≤ 10 | x > 10 | 绿/黄/橙/红 |

适用范围：最终表“风险水平*”列、Word 风险段落、监测点汇总表（BI 行用 BI 分级、ADI 行用 ADI 分级）。

**数值精度（按金标准校准）**：全程**保留源数据原精度，不做四舍五入取整**（源值如 8.33、11.11、2.22 原样保留）；仅 Word 日报与一览表的**显示**用 1 位小数（8.33 → 8.3）。SSI ×2 同样保留原精度。

### 3.6 日报 Word 生成

**文档版式（按示例文档「全省媒介伊蚊传染病疫情蚊媒监测情况（8月14日20：00）.docx」实测，完整排版规范见 3.6.1）：**

- **大标题**：`全省媒介伊蚊传染病相关蚊媒监测情况`（示例文档内部标题为“相关”，与文件名“疫情”不同，**按示例文档采用**；标题不含日期）。
- **副题行**（居中）：`（省疾控中心  {YYYY}年{M}月{D}日20:00）`——日期取用户输入（年份 4 位、月日不补零），时间固定 20:00（半角冒号，与示例一致）。
- **章节标题**：`一、布雷图指数（BI）调查评估。`／`二、成蚊密度（ADI）快速评估。`（示例文档由 Word 自动编号渲染“一、二、”，程序生成时直接在文本前写“一、”“二、”，视觉效果一致）。

**一、布雷图指数（BI）调查评估。**

概述段落（格式串）：

> {M}月{D}日，对{地市列表}共{地市数}个地市{区县数}个县区{镇街数}个镇街{监测点数}个村居/监测点开展了蚊媒密度应急监测。监测结果显示：{达标数}个监测点BI达标准要求，达标率为{达标率}%（{达标数}/{总数}）；{风险数}个监测点BI为低中高风险，占{风险占比}%（{风险数}/{总数}）。

- **地市列表**：最终 BI 表中出现的地市，按固定顺序（广州、深圳、珠海、汕头、佛山、韶关、河源、梅州、惠州、汕尾、东莞、中山、江门、阳江、湛江、茂名、肇庆、清远、潮州、揭阳、云浮）排列，名称不加“市”，顿号分隔，最后一项用“和”连接且**保留“市”**（例：`广州、深圳、佛山和肇庆市`）。某地市被排除字段部分排除时，其后加括号注记（例：`广州（荔湾区除外）`）。
- **地市数**：出现的地市个数。
- **区县数**：不重复 (地市, 区县) 组合数；**镇街数**：不重复 (地市, 街道) 组合数。
- **总数**：最终 BI 表记录数（达标率/风险占比的分母）。**监测点数**：最终 BI 表中**不重复“监测地点”的数量**（示例文档即 115 个监测点 / 116 条记录，二者不同）。
- **达标数** = BI < 5 的记录数；**风险数** = BI ≥ 5 的记录数。
- **达标率 / 风险占比** = 计数 ÷ 总数 × 100，保留 1 位小数，并附 `（{达标数}/{总数}）`。

风险列表段落（**高 → 中 → 低** 顺序；某等级数量为 0 则**不输出该段落**）：

> {风险等级}{数量}个：{元素1}、{元素2}……

- 元素格式：`{地市+市}{区县}{街道}{监测地点}（{BI值}）`，例：`广州市天河区石牌街道塘西村（核心区）（16.2）`。
- 区县为 `市辖区`（东莞/中山）时删除区县段位：`东莞市市辖区黄江镇` → `东莞市黄江镇…`。
- 组内排序：监测指标值**从高到低**。

**二、成蚊密度（ADI）快速评估。**

结构与“一”完全相同，数据源为最终 ADI 表，文字中 BI → ADI；达标 = ADI ≤ 2，风险 = ADI > 2，风险细分按 3.5 ADI 分级。

### 3.6.1 日报 Word 排版规范（按示例文档实测提取）

| 项目 | 设置 |
|---|---|
| 页面 | A4（21.0 × 29.7 cm）；页边距：上 3.70 cm、下 3.50 cm、左 2.80 cm、右 2.60 cm（公文样式） |
| 大标题 | 文本：`全省媒介伊蚊传染病相关蚊媒监测情况`；字体：**方正小标宋简体**（西文 Times New Roman，缺失时回退黑体）；字号：二号（22pt）；对齐：居中；行距：固定 36 磅；不加粗 |
| 副题行 | 文本：`（省疾控中心  2026年8月14日20:00）`（“中心”后两个空格；时间半角冒号；日期替换为用户输入）；字体：仿宋_GB2312（西文 Times New Roman）；字号：三号（16pt）；对齐：居中；行距：固定 28 磅；首行缩进 0 |
| 章节标题 | 文本：`一、布雷图指数（BI）调查评估。`／`二、成蚊密度（ADI）快速评估。`（“一、”“二、”直接写为文字）；字体：**黑体**；字号：三号（16pt）；对齐：默认（左对齐）；首行缩进：2 字符；行距：固定 28 磅；与下段同页（keepNext） |
| 正文段落（概述段、风险列表段） | 字体：**仿宋_GB2312**（西文 Times New Roman；缺失时回退 仿宋→宋体）；字号：三号（16pt）；对齐：**两端对齐**；首行缩进：2 字符；行距：固定 28 磅；字符间距：-0.3 磅（示例文档 w:spacing=-6 的等效值，可省略） |
| 空行 | 大标题与副题之间一个空行（行距 15 磅固定）；副题行与首章标题之间一个空行（行距 28 磅固定、首行缩进 2 字符） |
| 标点与数字 | 中文标点全角；阿拉伯数字、字母、百分号半角；风险列表元素间用“、”连接、段末用“。”；指标值保留 1 位小数（16.2）；百分数保留 1 位小数（89.7%） |
| 字体回退链 | 方正小标宋简体 → 黑体；仿宋_GB2312 → 仿宋 → 宋体；西文统一 Times New Roman |
| 实现说明 | 示例文档的章节号由 Word 自动编号（numId）渲染，程序直接写“一、”“二、”文字即可；示例文档末尾的制表符空行属人工痕迹，不生成 |

### 3.7 村居一览表 Excel（监测点汇总）

- **Sheet1「BI表」**：最终 BI 表数据。**Sheet2「ADI表」**：最终 ADI 表数据。**Sheet3「BI+ADI整合表」**：BI 与 ADI 按 (地市, 区县, 街道, 监测地点) 外连接整合（缺失项数值与风险均写 `/`，规则同 Word 一览表），列 = 地市、区县、街道、监测地点、ADI、风险水平*、BI*、风险水平*，格式同 BI/ADI 表（仅两个“风险水平*”列着色、地市列合并、所有单元格居中）。**Sheet4「删除数据情况说明」**：见 3.9。
- 列 = 地市、区县、街道、监测地点、BI*（ADI 表为 **ADI**）、风险水平*（文字 + 背景色按 3.5）。
  - BI*/ADI = 监测指标值（保留原精度）。
  - 地市/区县/街道按 3.2「监测点汇总表显示」口径（`天河区`→`天河`，`市辖区`→`/`）。
- **格式（所有 sheet）**：仅“风险水平*”**列**按风险等级着色（背景色参照一览表示例表格底纹：安全 `92D050` 绿 / 低风险 `FFFF00` 黄 / 中风险 `FFC000` 橘黄 / 高风险 `FF0000` 红），其余列不着色；“地市”列**纵向合并连续相同的单元格**；**所有单元格水平 + 垂直居中**。
- **字体字号**：所有文件中文 **仿宋_GB2312**、英文 **Times New Roman**；村居一览表内「BI+ADI整合表」字号**五号（10.5）**，其余 sheet 字号 **14**。
- **排序规则（BI表/ADI表/整合表）**：地市按固定顺序 → 区县升序 → 街道升序 → 监测地点升序。中文字符串升序按 Unicode 码点比较。
- **Sheet4「删除数据情况说明」**：见 3.9。

### 3.7.1 一览表 Word（**已取消输出**，D38）

> 该 Word 输出已按需求取消；BI+ADI 整合表改为村居一览表 Excel 内的 Sheet3（见 3.7），规则相同。

**原规范存档**：

**输出文件**：`全省媒介伊蚊传染病疫点重点镇（街道）蚊媒密度监测村居一览表（{M}月{D}日20：00）.docx`（全角冒号、月日不补零，与示例一致；与一览表 Excel 同名仅扩展名不同）。

**数据来源与合并规则**：
- 数据源：最终 BI 表 + 最终 ADI 表。
- 合并键 = (地市, 区县, 街道, 监测地点)。监测地点已含防控区类型（如“黄边社区（核心区）”），同一监测地点同时出现在两表中时合并为一行。
- **仅出现于 BI 表**：ADI 数值与 ADI 风险水平均写 `/`；**仅出现于 ADI 表**：BI* 数值与 BI 风险水平均写 `/`（与示例一致）。
- 防御处理：若同键在一侧仍有多行，取监测指标值最大的一行。
- 列顺序（与示例一致，ADI 在前）：地市 | 区县 | 街道 | 监测地点 | ADI | 风险水平* | BI* | 风险水平*。
- 地市/区县/街道/监测地点取值同 3.2「监测点汇总表显示」口径（地市不带“市”、区县去后缀）；**例外：东莞/中山的区县列显示 `-`**（示例如此，与监测点 Excel 的 `/` 口径不同）。
- ADI / BI* 数值保留原精度（Word 一览表显示时取 1 位小数）；风险水平按 3.5 分级文字（安全/低风险/中风险/高风险）。
- 排序：地市固定顺序 → 区县升序 → 街道升序 → 监测地点升序。

**排版规范（按示例文档「全省媒介伊蚊传染病疫点重点镇（街道）蚊媒密度监测村居一览表（8月20日20：00）.docx」实测提取）**：

| 项目 | 设置 |
|---|---|
| 页面 | A4；页边距：上 2.54 / 下 2.54 / 左 3.17 / 右 3.17 cm |
| 标题（两行，均居中） | 第 1 行：`全省媒介伊蚊传染病疫点`；第 2 行：`蚊媒密度监测情况`；字体：方正小标宋简体（回退黑体）；字号：二号（22pt） |
| 副题行（居中） | `（省疾控中心  {YYYY}年{M}月{D}日20:00）`；字体：楷体_GB2312（回退楷体）；字号：三号（16pt）；“中心”后两个空格、时间半角冒号、日期取用户输入（年份 4 位、月日不补零） |
| 表格 | 表头：仿宋_GB2312 五号（10.5pt）加粗、居中；数据行：仿宋_GB2312 五号（10.5pt）、**全部居中**；固定布局（tblLayout=fixed）；列宽 1.2 / 1.4 / 2.5 / 5.0 / 1.4 / 2.1 / 1.2 / 2.2 cm（生成时按页宽等比缩放以适配 14.66 cm 版心） |
| 表后注释 | 第 1 行 `* 注：`（宋体 12pt 加粗、两端对齐）；随后 5 行（仿宋_GB2312 12pt、两端对齐）：`1.省疾控中心飞行监测及市、县（区）疾控中心在两热疫点镇（街道）开展入户调查、蚊媒应急监测评估等工作。` `2.广州市幼虫监测如为标准间指数SSI按1:2换算成布雷图指数BI。` `3.风险水平分级标准为：` `安全范围：BI<5，或成蚊密度ADI≤2，符合蚊媒防控基本要求；` `低度风险：5≤BI<10，或成蚊密度2＜ADI≤5，存在传播潜在风险；` `中度风险：10≤BI<20，或成蚊密度5＜ADI≤10，提示可能引发本地疫情暴发；` `高度风险：BI≥20，或成蚊密度ADI＞10，发现病例易导致疫情暴发流行。` |
| 字体回退链 | 方正小标宋简体→黑体；楷体_GB2312→楷体；仿宋_GB2312→仿宋→宋体；西文统一 Times New Roman |

### 3.8 最小区分地址算法（可执行版）

适用：3.3 B8 / 3.4 A3。对每一组（地市-区/县/市-街道/乡/镇, 社区/村居, 防控区类型）相同但地址字段有差异的记录：

1. 取 `监测地址（地图定位版）`，为空则取 `监测地址（手填）`；两者皆空 → 该记录不改监测地点；若组内仍有无法区分的多条，按第 6 步回退。
2. 清理地址：循环删除其中与 `地市-区/县/市-街道/乡/镇` 完全相同的连续子串、与 `社区/村居` 相同的连续子串（先删长者）。
3. 按分隔符（中文逗号`，`、英文逗号`,`、顿号`、`、空格、句号`。`）切分，取第一个非空片段为候选；若候选以 `{地市名}市` 开头，去掉该前缀。
4. 若组内所有候选互不相同 → 候选即“最小区分地址”。
5. 若存在相同候选 → 逐步追加下一片段（候选1+候选2…），直至组内全部互异。
6. 片段用尽仍无法区分 → 保持原监测地点 `{社区/村居}（{防控区类型}）`，并对该组内冲突记录依次追加 `（1）`、`（2）`… 序号。
7. 监测地点 = `{社区/村居}{最小区分地址}（{防控区类型}）`。

**真实示例**（源数据：广州市番禺区东环街道 / 东环社区 / 警戒区，三条不同地址记录）：
- 地址1 `信业·悦都汇，GuangdongGuangzhouPanyu番禺大道北822号` → 候选 `信业·悦都汇`
- 地址2 `理想汽车广州番禺服务中心(龙美店)，…` → 候选 `理想汽车广州番禺服务中心(龙美店)`
- 地址3 `番禺电力科技园，广东省广州市番禺区东星路与莲花大道交叉口东南100米` → 候选 `番禺电力科技园`
- 结果：`东环社区信业·悦都汇（警戒区）`、`东环社区理想汽车广州番禺服务中心(龙美店)（警戒区）`、`东环社区番禺电力科技园（警戒区）`。

**需求原示例验证**：A 村两条记录，地址1 `F市B广场，C大街123号`、地址2 `A村D街道111号` → 地址2 删社区名“A村”得 `D街道111号`；地址1 候选 `F市B广场` → 去掉“F市”得 `B广场`。结果：`A村B广场（核心区）`、`A村D街道111号（核心区）`。

### 3.9 删除数据情况说明（监测点汇总 Sheet4）

- 列 = **与计算过程表一致的 11 列**（地市、区县、街道、地市-区/县/市-街道/乡/镇、社区/村居、监测地址（地图定位版）、监测地址（手填）、防控区类型、监测地点、监测方法、监测指标值）+ 新增列 `删除原因`。
- **仅包含输入日期数据的处理情况**：“监测时间为空”（无日期）的行不计入；监测时间不等于目标日期的行不计入。
- 新增列 **`监测日期`**（输入日期，位于删除原因前）。
- **排序**：按**删除原因**排列。
- 范围与原因清单（每行按处理顺序**取首个命中**的原因记录）：

| 删除原因 | 对应步骤 |
|---|---|
| 监测时间为空 | P2 |
| 监测指标值为空 | P3 |
| 被排除字段过滤 | P5 |
| 距末例天数不在范围内 | P6 |
| 防控区类型不符合 | P7 |
| 行政区划无法解析 | P9 |
| 监测方法不属于BI/SSI/ADI（不参与计算） | B1/A1 筛除的方法行 |
| 重复数据保留最大值 | B7 / A2 被删除的非最大值行 |

- **不列入**：监测时间不等于目标日期的行（数量庞大，无审计意义）。

### 3.10 计算过程 Excel 的 Sheet 组成（共 9 个）

| 序号 | Sheet 名 | 来源 | 标记 |
|---|---|---|---|
| 1 | SSI表 | B3 | 无 |
| 2 | BI+SSI(不重复) | B5 | SSI 来源行标黄 |
| 3 | BI+SSI(重复)+取较大值处理 | B6 | 无（含 3 个追溯列） |
| 4 | 重复数据删除(BI) | B7 | 被删行标红 |
| 5 | 地址区分处理(BI) | B8 | 被修改行标黄 |
| 6 | 最终表(BI) | B9 | 无 |
| 7 | 重复数据删除(ADI) | A2 | 被删行标红 |
| 8 | 地址区分处理(ADI) | A3 | 被修改行标黄 |
| 9 | 最终表(ADI) | A4 | 无 |

## 4. 输出文件与命名规范

| 文件 | 命名 | 备注 |
|---|---|---|
| 计算过程 Excel | `BI_ADI_计算过程_MM月DD日.xlsx` | MM/DD 两位补零 |
| 日报 Word | `全省媒介伊蚊传染病疫情蚊媒监测情况（{M}月{D}日20：00）.docx` | 按示例：**全省**前缀、月日**不补零**、**全角冒号**（半角 `:` 在 Windows 文件名非法） |
| 村居一览表 Excel | `村居一览表（{M}月{D}日20：00）.xlsx` | 已按需求改名；月日不补零、全角冒号 |

- 三份文件全部输出到用户选择的输出目录。
- 程序内使用文件**完整路径**读写（不经 shell），兼容含空格、中文、括号的路径。

## 5. GUI 要求（Tkinter）

- 输入总库表文件：浏览按钮 + 只读文本框；广州市表文件（可选，用于补全缺失的地市-区/县/市-街道/乡/镇，其余重合列完全一致方可补充）。
- 目标日期：年 / 月 / 日 三个下拉框（校验：月份 1–12；日按月份及闰年校验）。
- 排除字段：可选文本框（提示文案“如：荔湾区”）。
- 输出目录：选择按钮 + 文本框。
- 「开始处理」按钮（处理期间禁用，防重复点击）。
- 日志文本框：逐行追加当前步骤（“正在读取Excel…”→“正在筛选日期…”→“正在生成Word…”→“完成”）。
- 后台线程执行处理，避免界面卡死。
- 异常处理：文件不存在、非 xlsx、列缺失（列出缺失列名）、日期非法、日期无数据 → `messagebox` 报错后**停止本次处理但不退出程序**。

## 6. 异常与边界情况

- 输入日期无数据 → 弹窗“该日期无监测数据”，停止本次处理。
- 某步骤结果为空 → 对应 Sheet 只写表头。
- 数值精度：全程浮点计算，输出前统一四舍五入保留 1 位小数；用 Decimal(ROUND_HALF_UP) 实现（Python 内置 round 为银行家舍入，不满足“四舍五入”）。
- 日期解析：GUI 下拉框直接得年月日；如后续增加文本输入，支持 `yyyy/mm/dd` 与 `yyyy-mm-dd` 两种格式。
- 键值 K 比较时空值统一视为空字符串。
- 距末例天数允许负值/0（≤5 保留）。

## 7. 技术实现与打包

- **pandas**（读 Excel、数据处理）、**openpyxl**（写带格式 Excel：单元格填充色、列宽、Sheet 顺序）、**python-docx**（生成 Word）、**Tkinter**（GUI，标准库内置）。
- **PyInstaller** 打包单文件 exe；PyInstaller 不支持跨平台编译，**必须在 Windows 上构建** → GitHub Actions 用 `windows-latest` runner。
- **Win7 兼容**：构建使用 **Python 3.8** 并锁定兼容依赖（pandas 2.0.x、openpyxl 3.1.x、python-docx 1.x）。GitHub Actions 配置：`python-version: "3.8"`。
- 仓库需补齐：`requirements.txt`、`MosquitoMonitorTool.spec`（现有 `.github/workflows/build.yml` 已引用这两个文件）。
- 输出文件名用全角冒号，规避 Windows 非法字符。

## 8. 决策记录（本版已全部采纳）

| 编号 | 决策内容 |
|---|---|
| D1 | 行政区划解析按真实格式（无 `-`，广东省+地市+区县+街道拼接）重写 |
| D2 | Sheet3 不列入“监测时间不等于目标日期”的行 |
| D3 | 非 BI/SSI/ADI 方法（MOI/RI/BGs-Trap/双层叠帐/诱蚊灯/人诱）记入 Sheet3，原因“监测方法不属于BI/SSI/ADI” |
| D4 | “核心区和警戒区综合”等非核心/警戒区类型按不符合删除 |
| D5 | ADI 风险分级用 ADI 专属标准（≤2 安全 / 2-5 低 / 5-10 中 / >10 高），BI 用 <5 / 5-10 / 10-20 / ≥20 |
| D6 | 去重 Sheet（B7/A2）内容 = 全部行，被删行标红 |
| D7 | 地址区分 Sheet（B8/A3）内容 = 全部行，被修改行标黄，不加备注列 |
| D8 | B6 配对规则：SSI↔多条 BI 取 BI 最大值行配对；多条 SSI↔BI 取转换后最大值 |
| D9 | Word 风险等级数量为 0 时不输出该段 |
| D10 | 区县数/镇街数按 (地市,区县)、(地市,街道) 组合去重 |
| D11 | 达标率/风险占比保留 1 位小数 |
| D12 | 键值 K 中空地址字段视为空字符串 |
| D13 | 日期无数据 → 弹窗提示并停止本次处理，不关闭程序 |
| D14 | Sheet 名加 (BI)/(ADI) 后缀，避免两个“最终表”同名冲突 |
| D15 | Win7 兼容用 Python 3.8 构建 |
| D16 | 监测点表“防控区类型”仅内部参与排序，输出不含该列 |
| D17 | Sheet3 删除原因按处理顺序取首个命中 |
| D18 | 端州区特例一般化为“街道以区县名开头则去掉该前缀” |
| D19 | 日报版式按示例文档：大标题“全省媒介伊蚊传染病相关蚊媒监测情况”（不含日期），副题行“（省疾控中心  {YYYY}年{M}月{D}日20:00）”（日期取输入日期、月日不补零、时间半角冒号），其余内容结构与示例一致 |
| D20 | 字体回退链：标题 方正小标宋简体→黑体；正文 仿宋_GB2312→仿宋→宋体；西文统一 Times New Roman |
| D21 | 监测点数 = 最终表不重复“监测地点”数量；总数 = 记录数（达标率/风险占比分母） |
| D22 | 日报文件名按示例“全省…（{M}月{D}日20：00）.docx”（全省前缀、月日不补零、全角冒号） |
| D23 | 新增第 4 个输出“一览表 Word”（BI+ADI 整合表格），原有日报 Word、监测点汇总 Excel、计算过程 Excel 均保留 |
| D24 | 整合表合并键 = (地市, 区县, 街道, 监测地点)；仅 BI 或仅 ADI 的行，另一项数值与风险水平均写“/” |
| D25 | 整合表排版按示例文档实测（页边距 2.54/2.54/3.17/3.17、标题两行小标宋二号、副题楷体三号、表头/数据仿宋五号居中、表后注释、东莞/中山区县列“-”） |
| D26 | 一览表 Word 与一览表 Excel 命名统一（月日不补零、全角冒号，仅扩展名不同）；计算过程 Excel 保持 MM/DD 两位补零 |
| D27 | 数值保留源数据原精度，不做四舍五入取整；仅 Word 日报/一览表显示用 1 位小数（金标准校准） |
| D28 | SSI 仅当无同键 BI 时才 ×2 并入 BI 表；有同键 BI 时 SSI 行丢弃（不取大合并），Sheet3 红标追溯 |
| D29 | ADI 最终表/汇总表/一览表指标列名用 `ADI`（金标准与一览表示例如此，不用 ADI*） |
| D30 | 区县正则支持 `市` 后缀（阳春市、普宁市、罗定市、四会市、台山市等） |
| D31 | 地址区分仅对 (地市-区县-街道, 社区/村居, 防控区类型) 组内 ≥2 个不同地址的行改名；金标准中“塘头村居”（单地址组改名）与“城北社区”（值-地址错位）属其不一致行为，程序保持正确配对不复制 |
| D32 | 监测点汇总 BI表/ADI表格式：仅“风险水平*”列着色；地市列纵向合并相同值；所有单元格水平+垂直居中 |
| D33 | 监测点汇总新增 Sheet3「BI+ADI整合表」（规则同 Word 一览表，格式同 BI/ADI 表，两个风险水平列均着色） |
| D34 | 删除数据情况说明：列与计算过程表一致（11 列+删除原因）；仅含输入日期数据（“监测时间为空”不计入）；按监测方法排序 |
| D35 | 所有输出文件字体：中文 仿宋_GB2312、英文 Times New Roman（Word 与 Excel 均适用） |
| D36 | 村居一览表 Excel 改名“村居一览表（{M}月{D}日20：00）.xlsx”；字号：BI+ADI整合表五号(10.5)、其余 sheet 14 |
| D37 | （已由 D42 取代）风险水平背景色 |
| D38 | 取消“一览表 Word”输出，输出文件由 4 个减为 3 个 |
| D39 | 删除数据情况说明：新增“监测日期”列；按删除原因排列 |
| D40 | 计算过程 Excel「BI+SSI(重复)+取较大值处理」sheet 背景设为无颜色 |
| D41 | 广州市表补全：总库中广州市缺失“地市-区/县/市-街道/乡/镇”且与广州表其余重合列完全一致（唯一匹配）时按广州表补充；未匹配行按行政区划无法解析处理 |
| D42 | 风险水平背景色参照一览表示例表格底纹：安全 92D050 / 低风险 FFFF00 / 中风险 FFC000 / 高风险 FF0000 |
