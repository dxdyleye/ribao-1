# 蚊媒监测数据处理程序 —— 筛选与数据处理规则

> 适用版本：程序 v4.x（对应《需求文档_最终版.md》D48–D59 全部规则）。
> 本文档说明程序从输入文件到输出文件的完整筛选与数据处理规则，供操作与核对使用。

---

## 1. 程序概述

程序读取**总库表**（必需）、**广州市表**（可选，总库中广州市数据以此为准，D70）与**飞行监测表**（可选），按输入的目标日期筛选处理，最终输出 **4 个文件**：

| 输出 | 文件名 |
|---|---|
| 日报 Word | `全省媒介伊蚊传染病疫情蚊媒监测情况（{M}月{D}日20：00）.docx` |
| 村居一览表 Excel | `村居一览表（{M}月{D}日20：00）.xlsx` |
| 计算过程 Excel | `BI_ADI_计算过程_MM月DD日.xlsx` |
| 基础数据集 Excel | `基础数据集_MM月DD日.xlsx` |

全部文件输出到用户所选目录下的 **`省蚊媒监测日报（{M}月{D}日）`** 文件夹中（月日不补零），处理完成后程序自动打开该文件夹。

---

## 2. 输入与界面操作

### 2.1 总库表（唯一必需输入）
- 界面要求：**需包含所有监测日期**（即含该监测点的完整历史监测记录，用于 P6 乱码规则判定“最初监测日期”）。

### 2.1.1 广州市表（可选输入，D70）
- 界面要求：**需包含所有监测日期**。
- 提供后，**总库中“广州市”相关数据以广州市表为准**：先剔除总库中地市为广州的行（按“地市”列或“地市-区/县/市-街道/乡/镇”解析判定），再并入广州市表的行；**其余地市数据处理不变**。
- 广州市表通常**没有**“距首例/末例天数（自动计算）”列，程序按下述规则补算（以输入的目标日期为基准）：
  1. 距末例天数 = 输入日期 − 末例病例报告时间（天）；
  2. 无末例时间 → 距末例天数 = 输入日期 − 首例病例报告时间（天）；
  3. 首末例时间都没有 → 距末例天数与距首例天数均置 **40001**（>40000，触发 P6 乱码规则：按“该点最后病例报告时间”到输入日期的间隔判定，D71）；
  4. 距首例天数 = 输入日期 − 首例病例报告时间（天），无首例则置 40001；
  5. 最终按 P6 保留**距末例天数 ≤5**（或乱码规则下间隔 ≤5）的数据。
- 必需列（列名完全匹配，顺序不限）：

| 列名 | 说明 |
|---|---|
| 监测时间（年/月/日） | 日期（可为 datetime） |
| 监测指标值 | 数值 |
| 地市-区/县/市-街道/乡/镇 | 实测为“广东省+地市+区县+街道”直接拼接 |
| 距末例天数（自动计算） | 数值 |
| 防控区类型 | 核心区/警戒区/监控区/重点场所等 |
| 社区/村居 | 文本 |
| 监测方法（如BI/RI/MOI/ADI等） | 布雷图指数BI / 标准间指数SSI / 成蚊密度指数法ADI 等 |
| 监测地址（地图定位版） | 可空 |
| 监测地址（手填） | 可空 |

- 另有“距首例天数（自动计算）”列：**P6 乱码规则需要**；源表缺该列时 P6 对“末例>40000”部分退回简化规则。
- 缺必需列时弹窗列出缺失列并停止处理。

### 2.2 飞行监测表（可选）
- 范表 `(2026疫点飞行监测)飞行监测-0821`；必需列：监测时间、地市-区/县/市-街道/乡/镇、社区/村居、防控区类型、监测地址（地图定位版）、监测地址（手填）、`ADI值（自动计算）`、`BI值（自动计算）`。

### 2.3 目标日期
- 年/月/日三个下拉框；校验月份 1–12、日按月份与闰年校验；该日期无数据时提示“该日期无监测数据”并停止。

### 2.4 排除字段（可选，两列设计）
- 每个排除字段一行：标题“排除字段N”+ 第一列输入框（列名：区/县/市-街道/乡/镇）+“+”+ 第二列输入框（列名：社区/村居）+ 右侧“+”按钮（添加下一字段）。
- 输入框上方有列说明文字“**地市-区县-街道**”“**社区村居**”。
- 匹配规则：
  - **字段内**：两列都填时为“与”（两列均命中才删除）；只填一列时按该列匹配；都不填则该字段无效；
  - **字段间为“或”**：任一字段命中即删除。
- 说明文案：“排除字段应按列名填入，如‘罗定市素龙街道’+‘平南村委’或‘荔湾区’+‘’”。
- 注：匹配范围为“地市-区/县/市-街道/乡/镇”与“社区/村居”两列；**手填地址/地图定位地址列不参与排除匹配**。

### 2.5 输出目录
- 选择或默认取输入文件所在目录；程序在其下创建“省蚊媒监测日报（M月D日）”文件夹存放 4 个输出文件。

---

## 3. 全局预处理（按顺序执行，P1–P10）

> 每步被删除的行会记入“删除数据情况说明”（仅记输入日期当天的数据；删除原因取首个命中）。

| 步骤 | 操作 | 删除原因 |
|---|---|---|
| P1 | 筛选 监测时间(date 部分) == 目标日期 的记录 | （日期不符不记录） |
| P2 | 删除监测时间为空的记录 | 监测时间为空 |
| P3 | 删除监测指标值为空的记录（数值保留源精度，不取整） | 监测指标值为空 |
| P5 | 排除字段匹配（见 2.4）：任一字段命中即删除 | 被排除字段过滤 |
| P6 | 距末例天数：≤5 或 >40000 保留（负值、0 视为 ≤5）；其中 >40000 的记录仅保留 距首例天数 ≤5 或 >40000 的；**末例>40000 且 首例>40000（乱码）** 的记录按“该点最后病例报告时间”（D71，见下）判定，该点无任何病例报告时间时退回原方案并予保留（D72） | 距末例天数不在范围内 |
| P7 | 仅保留 防控区类型 ∈ {核心区, 警戒区} | 防控区类型不符合 |
| P8 | 新增列 监测地点 = `{社区/村居}（{防控区类型}）` | — |
| P9 | 解析新增列 地市、区县、街道（见 §4） | 行政区划无法解析 |
| P10 | 预处理完成 → **基础数据集**（另输出基础数据集 Excel，见 §8.3） | — |

### P6 乱码规则（“最初监测时间”判定，D71）
- 适用：距末例天数 > 40000 **且** 距首例天数 > 40000 的记录（两值均为异常大数，通常即该行 首例/末例病例报告时间为空）。
- 判定（**优先方案，D71**）：
  1. 取**该点有记录的最后病例报告时间**：按 (地市-区/县/市-街道/乡/镇, 社区/村居) 归点，在该点全部记录的 `首例病例报告时间` 与 `末例病例报告时间` 两列中取**不晚于输入日期**的最大值，作为该点“最初监测时间”；
  2. 计算该时间到**输入日期**的间隔天数：**≤ 5 天纳入、> 5 天放弃**。
- 兜底（该点在输入日期及之前没有任何病例报告时间，**经评审确认退回原方案、仍予保留，D72**）：退回原方案——
  1. 从 **P1 处理前**的源数据中，取该点**对应监测方法**且**监测指标值不为空**的记录；
  2. 取这些记录监测日期序列中**最近一次中断（相邻日期差 > 1 天）之后重新开始的日期**作为“最初监测日期”；
  3. 同样按 ≤5 天纳入、>5 天放弃（即**不因缺少病例报告时间而整体排除该点**；多数此类点为仅一条记录的新点，最初监测日期＝输入日期、间隔 0，正常保留）。
- 被放弃的行记入“删除数据情况说明”（原因：距末例天数不在范围内），并在“基础数据集”的乱码处理 sheet 中以**浅红色**标出（`最初监测日期` 列显示实际采用的“最初监测时间”）。
- 规则依据：出现新病例后连续 5 天监测、之后每 3–5 天一次、总共至少 22 天；正常记录的纳入判据为“监测日期 − 末例日期 ≤ 5 天”，乱码记录因两列天数不可用，改以该点最后病例报告时间为基准等价判定。

---

## 4. 行政区划解析（地市/区县/街道）

- 输入串 S = `广东省{地市}市{区县}{区/县/市}{街道}{街道/镇/乡}`（无分隔符拼接）。
- 规则：去掉前缀“广东省”→ 按 21 地市固定顺序匹配 `{地市名}市` → 用正则 `^(市辖区|.+?区|.+?县|.+?市)` 取区县前缀 → 余下为街道；若街道以区县名开头（如“端州区黄岗街道”）则去掉该前缀。
- **回退（D64）**：上述主规则在街道名中含“区/县/市”字（如“……高新技术产业开发区”）时可能把整串误当区县、街道为空而失败；此时取去掉地市后剩余串中**最短**的“区/县/市”结尾前缀为区县，街道 = 剩余字段（例：`肇庆市四会市肇庆高新技术产业开发区` → 区县=四会市、街道=肇庆高新技术产业开发区）。
- 无法解析（非“广东省”开头等）→ 删除该行。

---

## 5. BI + SSI 专项处理（B1–B9）

1. **B1 取数**：监测方法 ∈ {布雷图指数BI, 标准间指数SSI} 的行进入 BI 处理；其他方法（MOI/RI/BGs-Trap/双层叠帐/诱蚊灯/人诱等）记入删除说明（不参与计算）。
2. **B2 SSI 换算**：SSI 行指标值 ×2（保留原精度）得“转换后SSI值”。
3. **B3 SSI表**：全部 SSI 行（转换后值）。
4. **B4 键 K** = (地市-区/县/市-街道/乡/镇, 社区/村居, 监测地址（地图定位版）, 监测地址（手填）, 防控区类型)，空值视为空串。
5. **B5 SSI 归属**：
   - 无同键 BI → SSI 行改方法为 BI、值用转换后值，**并入** BI 数据集（来源行标黄）；
   - 有同键 BI → **不并入**；若转换后 SSI 值 **< 5** → 保留原 BI 值；若 **≥ 5** → 该 K 的最终 BI 值 = **原BI值与转换后SSI值的较大者**（提升 BI 行值）。
6. **B6 同键追溯**：被丢弃的同键 SSI 行集中到 Sheet3「BI与SSI重复处理」，列 = Sheet1 列 + `原BI值、原SSI值、转换后的SSI值、最终BI值`。
7. **B7 去重**：按 K 分组，每组保留指标值最大的行（被删行标红，记删除说明“重复数据保留最大值”）。
8. **B8 地址区分**：对 (地市-区/县/市-街道/乡/镇, 社区/村居, 防控区类型) 组内存在 ≥2 个不同地址的行，按“最小区分地址”改名（见 §6；被修改行标黄）。
9. **B9 最终 BI 表**：列 = 地市、区县、街道、监测地点、`BI*`、`风险水平*`（BI 分级）。

---

## 6. 最小区分地址算法（地址区分）

- 对每组（地市-区/县/市-街道/乡/镇, 社区/村居, 防控区类型）内地址字段有差异的记录：
  1. **分别**以“监测地址（地图定位版）优先”与“监测地址（手填）优先”两种来源计算最小区分地址；
  2. 清理地址：循环删除与“地市-区/县/市-街道/乡/镇”、社区/村居 相同的连续子串（先删长者）；并删除残留的省级“广东省”（D67，如 `立德村（广东省）` → 应为 `立德村（坟埕巷（老厝））`）；
  3. 按分隔符（，,、空格。）切分，取第一个非空片段为候选；候选以“{地市名}市”开头则去掉该前缀；
  4. 组内候选互不相同 → 即为“最小区分地址”；相同则逐步追加下一片段直至互异；
  5. **最终值 = 两种来源结果中字段较短者**（等长取地图定位版）；混选后仍冲突的记录追加（1）（2）… 序号；
  6. 监测地点 = `{社区/村居}（{最小区分地址}）（{防控区类型}）`。
  7. **删除“工作地：/居住地：”前缀（D66）**：两监测地址字段出现“工作地：/居住地：”前缀字样时，最终监测地点中删除此前缀（如 `大镇社区（工作地：智讯电子厂）（核心区）` → `大镇社区（智讯电子厂）（核心区）`）。

---

## 7. ADI 专项处理（A1–A4）

1. **A1 取数**：监测方法 == 成蚊密度指数法ADI 的行进入 ADI 处理。
2. **A2 去重**：按 K 分组保留指标值最大行（被删行标红）。
3. **A3 地址区分**：同 §6。
4. **A4 最终 ADI 表**：列 = 地市、区县、街道、监测地点、`ADI*`、`风险水平*`（ADI 分级）。

---

## 8. 飞行监测表处理（F1–F5）

1. **F1 校验**：8 个必需列缺失则弹窗报错。
2. **F2** 参考 P1/P2/P3/P8：筛选目标日期、删空时间、删空指标值（BI 流取 `BI值（自动计算）`、ADI 流取 `ADI值（自动计算）`，分别判定）；新增列 监测地点 = `{社区/村居}（{防控区类型}，飞行监测）`。
3. **F3** 行政区划解析（失败行删除）。
4. **F4** 按 A2 完整键去重，保留最大值。
5. **F5** 提取 `ADI值（自动计算）`、`BI值（自动计算）`，分别按 ADI/BI 分级生成行，**整合入最终 ADI 表与最终 BI 表**（同步更新计算过程「最终表(ADI)/(BI)」），供日报与一览表使用；飞行行不进入删除说明。

---

## 9. 风险分级

| 方法 | 安全 | 低风险 | 中风险 | 高风险 |
|---|---|---|---|---|
| BI | x < 5 | 5 ≤ x < 10 | 10 ≤ x < 20 | x ≥ 20 |
| ADI | x ≤ 2 | 2 < x ≤ 5 | 5 < x ≤ 10 | x > 10 |

- 数值保留源精度；日报/一览表**显示**用 Decimal 四舍五入保留 1 位（13.85→13.9）。

---

## 10. 排序规则（村居一览表 / 计算过程 / 基础数据集）

所有数据表统一按以下键排序（中文字符串按拼音序，GBK 字节序近似实现）：

```
地市固定顺序(广州→深圳→…→云浮) → 区县升序 → 街道升序 → 社区/村居 → 防控区类型(核心区→警戒区→其他) → 监测地点升序
```

- 同一社区的行聚在一起，社区内按 核心区 → 警戒区 排列。

---

## 11. 日报 Word 规则

- **大标题**：`全省媒介伊蚊传染病相关蚊媒监测情况`（居中、二号）；
- **副题行**：`（省疾控中心  {YYYY}年{M}月{D}日20:00）`；
- **章节**：`一、布雷图指数（BI）调查评估。` / `二、成蚊密度（ADI）快速评估。`；
- **概述段**：`{M}月{D}日，对{地市列表}共{地市数}个地市{县区数}个县区{镇街数}个镇街{监测点数}个村居/监测点开展了蚊媒密度应急监测。监测结果显示：{达标数}个监测点…达标率为{达标率}%（…）；{风险数}个监测点…占{风险占比}%（…）。`
  - 地市列表按固定顺序，末项保留“市”；被排除字段部分排除的地市加括号注记（多字段提取公共部分置前、其余按输入顺序以“、”相隔，如 `云浮（罗定市素龙街道平南村委、素龙街道龙岗花园、罗城街道区屋居委除外）`）；
  - 县区数 = 不重复区县名（**东莞/中山“市辖区”不计算在内**，D69）；镇街数 = (地市,街道) 去重；监测点数 = (街道,监测地点) 去重；总数 = 记录数；
  - BI 达标 = BI < 5；ADI 达标 = ADI ≤ 2。
- **风险列表段**：高 → 中 → 低；某等级数量为 0 不输出；元素 `{地市+市}{区县}{街道}{监测地点}（{值}，第{N}天）`（N=监测天数，规则见 §12.1），区县为“市辖区”（东莞/中山）时删除区县段位；组内按值从高到低。
- **nan 标红**：文本中出现 `nan` 字样（源数据社区/地址等字段缺失导致）时，对该“nan”文字单独设置红色背景（填充色 FF0000），其余文字正常。

---

## 12. 输出格式与文件明细

### 12.1 村居一览表 Excel（6 sheet）
| Sheet | 内容 | 列 |
|---|---|---|
| BI表 | 最终 BI 表 | 地市、区县、街道、监测地点、BI* |
| ADI表 | 最终 ADI 表 | 地市、区县、街道、监测地点、ADI* |
| BI+ADI整合表 | 按 (地市,区县,街道,监测地点) 外连接整合 | 地市、区县、街道、监测地点、ADI*、BI* |
| 地址字段过长过短-供审核 | 过长段：整合表中 社区/村居 ≥6 汉字 或最终 区县/街道 汉字数≥6；空一行后过短段：最终 区县或街道 字数≤1 含空（浅红 FFC7CE，区县“-”市辖区除外） | 地市、区县、街道、监测地点、ADI*、BI* |
| 地址-供审核 | 整合表中“地市/区县/社区村居”空白条目 + 经过最小地址区分处理（§11）的行，两部分之间空一行 | 地市、区县、街道、监测地点、ADI*、BI*、监测地点（地图）、监测地点（手填）、审核原因 |
| 删除数据情况说明 | 见 §13 | — |

- 区县按显示口径（去“市/县/区”后缀；东莞/中山“市辖区”显示“-”；**例外：原始区县为“城区”时保留“城区”，不去“区”字**，D68）。
- **监测天数**（D63）：监测天数 N 以**距末例天数**为准；若距末例天数>40000 则以**距首例天数**；若距首例天数也>40000 则用该点“最初监测时间”（D71：该点最后的病例报告时间；该点无病例报告时间时退回“最近一次中断后重新开始的监测日期”，D72）到输入日期的间隔天数。**BI 表（BI≥5）/ADI 表（ADI>2）超限行**的“监测地点”追加“（第N天）”；**整合表**先按基础监测地点（不含天数）外连接，展示监测地点以“有监测天数的侧”为准——两侧都超限且天数不同取**天数较大**者，仅一侧超限用该侧，均不超限不带天数。
- **整合表同点合并（D65）**：BI 与 ADI 侧 (地市,区县,街道,社区/村居,防控区类型) 一致，且基础数据集“监测地址（地图定位版）”“监测地址（手填）”两列均一致，即使“监测地点”文本不同（如 BI“龙田社区（核心区）” vs ADI“龙田社区（阳光锦府）（核心区）”）也视为同一监测点合并到整合表同一行，该行监测地点取**字段较长**者。
- **格式**：BI*/ADI* 数值列按风险等级着色（安全 92D050 绿 / 低 FFFF00 黄 / 中 FFC000 橘 / 高 FF0000 红）；整合表缺失项 `/` 背景同安全绿；**整合表中出现“nan”字样的行，其 区县/街道/监测地点 三列背景标红（FF0000），其余列保持原格式**；**地址-供审核**：空白条目审核原因“地址列存在空白”，最小地址区分行 区县/街道/监测地点 三列浅红 FFC7CE、审核原因“经过最小地址区分，需要审核”；BI表/ADI表字号 14，**整合表小四(12)、标题行加粗、有内容单元格全框线**；地市列纵向合并；所有单元格居中；中文字体 仿宋_GB2312、英文 Times New Roman。
- 排序见 §10。

### 12.2 计算过程 Excel（9 sheet，均按 §10 排序）
`SSI表、BI+SSI(不重复)、BI与SSI重复处理、重复数据删除(BI)、地址区分处理(BI)、最终表(BI)、重复数据删除(ADI)、地址区分处理(ADI)、最终表(ADI)`。
- 标记：SSI 来源行黄、被删行红、被修改行黄；BI与SSI重复处理含 `原BI值/原SSI值/转换后的SSI值/最终BI值` 四追溯列。

### 12.3 基础数据集 Excel（4 sheet）
| Sheet | 内容 |
|---|---|
| BI基础数据集 | 基础数据集中的 BI/SSI 方法行，列 = 地市、地市-区/县/市-街道/乡/镇、区县、社区/村居、监测地点、监测地址（地图定位版）、监测地址（手填）、监测时间、监测方法、监测指标值 |
| ADI基础数据集 | 基础数据集中的 ADI 方法行，同上列 |
| 距首例和末例天数乱码处理（BI） | 乱码记录（末例>40000 且 首例>40000），在上列基础上新增 `最初监测日期`（D71：该点最后的病例报告时间；无病例报告时间时退回原方案，D72）、`距输入日期天数`；被放弃（间隔>5）的行**浅红** |
| 距首例和末例天数乱码处理（ADI） | 同上（ADI） |

- 均按 §10 排序。

---

## 13. 删除数据情况说明（村居一览表 Sheet5）

- 列 = 地市、区县、街道、地市-区/县/市-街道/乡/镇、社区/村居、监测地址×2、防控区类型、监测地点、监测方法、监测指标值、监测日期、删除原因。
- 仅包含输入日期当天的数据（“监测时间为空”的行不计入；日期不符的行不计入）；按删除原因排序。
- 删除原因清单（按处理顺序取首个命中）：监测时间为空、监测指标值为空、被排除字段过滤、距末例天数不在范围内、防控区类型不符合、行政区划无法解析、监测方法不属于BI/SSI/ADI（不参与计算）、SSI记录与同键BI记录重复、重复数据保留最大值。

---

## 14. 异常与边界

- 输入日期无数据 → 弹窗“该日期无监测数据”，停止本次处理但不退出；
- 文件缺失/被移动/云占位 → 弹窗给出完整路径与同目录文件列表，提示网盘文件先下载到本地；
- 排除字段只匹配两列（地市-区/县/市-街道/乡/镇、社区/村居），不匹配地址列；
- 键值 K 比较时空值统一视为空字符串；
- 距末例天数允许负值/0（≤5 保留）；
- 某步骤结果为空 → 对应 sheet 只写表头。
