# 蚊媒监测数据处理程序 —— 需求文档（修订版 v2.0）

> 本版依据原始需求 + 实际输入文件「(总表地市数据汇总表)总库-0814 .xlsx」实测结果修订。
> 实测事实：源文件单 Sheet「总库」，32 列，19956 行；`地市-区/县/市-街道/乡/镇` 全表无 `-` 分隔符；监测方法 9 种；防控区类型 5 种；监测时间为 datetime 类型。
> 文中【决策 Dn】为修订时替用户拍板的假设，见第 8 节决策清单，确认后方可实施。

---

## 1. 总体目标

开发一个 Windows 可执行程序（.exe），具备图形用户界面（GUI）。用户通过界面选择 Excel 源文件、输入目标日期（年月日）及排除字段（可选），程序自动处理数据，生成三份文件：

1. 一份日报 Word（.docx）
2. 一份监测点汇总 Excel（.xlsx）
3. 一份计算过程 Excel（.xlsx）【补充：原始需求第 1 节第三项缺失，按第 4 节补全】

程序兼容主流 Windows 系统，使用 Python 开发，GitHub Actions 打包为 exe。

## 2. 输入数据约定

### 2.1 必需列（10 列，列名完全匹配，顺序不限）

| 列名 | 说明 | 可否为空 |
|---|---|---|
| 监测时间（年/月/日） | 日期，实测为 datetime 类型 | 否（为空则删除） |
| 监测指标值 | 数值 | 否（为空则删除） |
| 地市-区/县/市-街道/乡/镇 | 文本，**实测无 `-` 分隔符**（见 2.2） | 否 |
| 距末例天数（自动计算） | 数值，实测范围 -2 ~ 46263 | 否 |
| 防控区类型 | 文本：核心区 / 警戒区 / 监控区 / 重点场所 / 核心区和警戒区综合 | 否 |
| 社区/村居 | 文本 | 否 |
| 监测方法（如BI/RI/MOI/ADI等） | 文本，实测 9 种（见 2.2） | 否 |
| 监测地址（地图定位版） | 文本 | 可空 |
| 监测地址（如“监测地址”定位字段不可用，可手填；如定位可用，不需要重复填写） | 文本 | 可空 |

### 2.2 真实数据格式说明（实测）

- `地市-区/县/市-街道/乡/镇` 实际为“广东省+地市+区县+街道”**直接拼接、无任何分隔符**，格式：
  `广东省{地市}市{区县}{区/县/市}{街道}{街道/镇/乡}`
  例：`广东省广州市白云区人和镇`；`广东省东莞市市辖区黄江镇`；`广东省肇庆市端州区端州区黄岗街道`（端州区在街道部分**重复出现**）。
- 监测方法共 9 种：`布雷图指数BI`、`标准间指数SSI`、`成蚊密度指数法ADI`、`诱蚊诱卵器指数MOI`、`路径指数RI`、`BGs-Trap/化学信息素诱捕法`、`双层叠帐法`、`诱蚊灯法`、`人诱`。
- 防控区类型共 5 种：`核心区`、`警戒区`、`监控区`、`重点场所`、`核心区和警戒区综合`。
- 监测时间列为 datetime（含 00:00:00 时间部分），**日期比较只取 date 部分**。

### 2.3 列校验与多余列处理

- 校验：上述 10 列必须**全部存在且列名完全匹配**；否则弹窗列出缺失列名并友好退出。
- 源文件多余的 22 列（含 `地市-区/县/市-街道/乡/镇2`、`环境类型`、`备注` 等）**一律忽略**，不参与处理、不参与输出（Sheet3 删除说明除外，见 3.9）。

## 3. 数据处理流程

### 3.1 全局预处理（按顺序执行）

| 步骤 | 操作 | 删除原因（供 Sheet3） |
|---|---|---|
| P1 | 筛选 监测时间(date 部分) == 目标日期 的记录 | 不列入 Sheet3（见 3.9） |
| P2 | 删除监测时间为空的记录 | 监测时间为空 |
| P3 | 删除监测指标值为空的记录 | 监测指标值为空 |
| P4 | 所有监测指标值四舍五入**保留 1 位小数**（用 Decimal ROUND_HALF_UP，避免 Python round 银行家舍入） | — |
| P5 | 若用户输入排除字段：删除 `地市-区/县/市-街道/乡/镇` 字符串**包含**该字段的记录（子串匹配；字段与单元格均 strip 后匹配，例：输入“荔湾区”删除所有含“荔湾区”的行） | 被排除字段过滤 |
| P6 | 保留 距末例天数 ≤ 5 **或** > 40000 的记录（负值、0 均视为 ≤5 保留） | 距末例天数不在范围内 |
| P7 | 仅保留 防控区类型 ∈ {核心区, 警戒区} | 防控区类型不符合 |
| P8 | 新增列 监测地点 = `{社区/村居}（{防控区类型}）` | — |
| P9 | 新增列 地市、区县、街道（解析规则见 3.2） | 行政区划无法解析 |
| P10 | 预处理完成 → 得到 **基础数据集** | — |

### 3.2 地市 / 区县 / 街道 解析规则（真实格式，替代原“按 `-` 拆分”）

输入串 S = `广东省{地市}市{区县}{区/县/市}{街道}{街道/镇/乡}`：

1. S 为空或非空但不以 `广东省` 开头 → 无法解析，该行删除，原因“行政区划无法解析”（实测仅 43 个空值行，无其他情况）。
2. 去掉前缀 `广东省`。
3. 按固定 21 地市顺序（广州、深圳、珠海、汕头、佛山、韶关、河源、梅州、惠州、汕尾、东莞、中山、江门、阳江、湛江、茂名、肇庆、清远、潮州、揭阳、云浮）逐一尝试：若剩余部分以 `{地市名}市` 开头 → **地市 = 该地市名**（不含“市”），并去掉 `{地市名}市`。
4. 区县：从剩余部分开头用正则 `^(市辖区|.+?区|.+?县)` 匹配取前缀（例：`白云区人和镇`→`白云区`；`封开县平凤镇`→`封开县`；`市辖区黄江镇`→`市辖区`）。
5. 街道 = 剩余部分（含 街道/镇/乡 后缀，原样保留）。
6. **端州区特例（一般化规则）**：若街道以区县名开头，去掉该前缀。例：`端州区黄岗街道` → `黄岗街道`（原需求第 18 条的意图，实测肇庆端州区 2191 行街道部分均重复“端州区”）。
7. 三种输出口径：
   - **内部字段**（计算/排序用）：地市不带“市”（`广州`）；区县原样（`天河区`、`市辖区`）；街道原样。
   - **监测点汇总表显示**：地市同内部；区县去掉末尾“市/县/区”字（`天河区`→`天河`、`封开县`→`封开`），`市辖区`→`/`；街道原样。
   - **Word 风险列表显示**：地市 = 内部地市 + `市`（`广州市`）；区县原样（`天河区`、`封开县`），**`市辖区` 时整个区县段位删除**（“东莞市市辖区” → “东莞市”）；街道原样。

### 3.3 BI + SSI 专项处理

**B1** 从基础数据集筛选 监测方法 ∈ {`布雷图指数BI`, `标准间指数SSI`} → **BI 数据处理表**（内存中间表，不输出为 Sheet）。

**B2** SSI 转换：SSI 行 监测指标值 × 2，结果再保留 1 位小数，得“转换后SSI值”。

**B3** Sheet1「SSI表」：全部 SSI 行（转换后值）。列 = 地市、区县、街道、地市-区/县/市-街道/乡/镇、社区/村居、监测地址（地图定位版）、监测地址（手填）、防控区类型、监测地点、监测方法、监测指标值（转换后）。

**B4** 判定“一致”的键值 K = (地市-区/县/市-街道/乡/镇, 社区/村居, 监测地址（地图定位版）, 监测地址（手填）, 防控区类型)。**空值一律视为空字符串**参与比较。【决策 D12】

**B5** 不重复合并：SSI 行若与**所有** BI 行在 K 上不完全一致（任一字段不同）→ 监测方法改为 `布雷图指数BI`、指标值用转换后 SSI 值、其余字段不变，合并入 BI 数据集。
→ Sheet2「BI+SSI(不重复)」：内容 = 原始 BI 行 + 上述合并行；**来自 SSI 的行背景填充黄色**。

**B6** 完全一致取大：SSI 行若与**某条** BI 行在 K 上完全一致 → 合并为一行：指标值 = max(该 BI 值, 转换后 SSI 值)，监测方法 = `布雷图指数BI`。
配对规则【决策 D8】：一条 SSI 对应多条 BI 时，取 BI 中指标值最大者配对；多条 SSI 对应一条 BI 时，取转换后值最大者参与合并。
→ Sheet3「BI+SSI(重复)+取较大值处理」：列 = Sheet1 列 + 新增三列 `原BI值`、`原SSI值`、`转换后的SSI值`。

**B7** 完整键去重：对全部 BI 记录（原始 BI + B5 合并 + B6 合并）按 K 分组，**每组只保留指标值最大的行**，其余删除。
→ Sheet4「重复数据删除(BI)」【决策 D6】：内容 = **全部行**（保留行 + 被删行），被删除行（每组非最大值行）**背景标红**。

**B8** 地址区分：对 B7 结果中 `(地市-区/县/市-街道/乡/镇, 社区/村居, 防控区类型)` 三者相同、但任一地址字段不同的组，修改 监测地点 = `{社区/村居}{最小区分地址}（{防控区类型}）`（算法见 3.8）。
→ Sheet5「地址区分处理(BI)」【决策 D7】：内容 = 全部行，**被修改行背景标黄**（不新增备注列，原地址在“监测地址”两列中可回溯）。

**B9** 最终 BI 表 = B8 结果。列 = 地市、区县、街道、监测地点、BI*、风险水平*（风险分级见 3.5）。
→ Sheet6「最终表(BI)」。（地市/区县/街道列即按 3.2 解析结果，监测点表直接复用，不再二次解析）【决策 D14：Sheet 名加“(BI)/(ADI)”避免同名冲突】

### 3.4 ADI 专项处理

**A1** 从基础数据集筛选 监测方法 == `成蚊密度指数法ADI` → **ADI 数据处理表**（内存中间表）。

**A2** 完整键去重（K 同 B4）：每组保留指标值最大的行，其余删除。
→ Sheet1「重复数据删除(ADI)」：内容 = 全部行，被删行背景标红。

**A3** 地址区分（同 3.8 算法）。
→ Sheet2「地址区分处理(ADI)」：内容 = 全部行，被修改行背景标黄。

**A4** 最终 ADI 表。列 = 地市、区县、街道、监测地点、ADI*、风险水平*（ADI 分级）。
→ Sheet3「最终表(ADI)」。
（修订：原需求 21.5 写“将步骤 21.5 处理完的数据”应为 21.4；列名 `BI*` 应为 `ADI*`。）

### 3.5 风险分级表（统一按此表，修订两套分级冲突）

| 方法 | 安全 | 低风险 | 中风险 | 高风险 | 背景色 |
|---|---|---|---|---|---|
| BI（布雷图指数） | x < 5 | 5 ≤ x < 10 | 10 ≤ x < 20 | x ≥ 20 | 绿/黄/橙/红 |
| ADI（成蚊密度） | x ≤ 2 | 2 < x ≤ 5 | 5 < x ≤ 10 | x > 10 | 绿/黄/橙/红 |

适用范围：最终表“风险水平*”列、Word 风险段落、监测点汇总表（BI 行用 BI 分级、ADI 行用 ADI 分级）。【决策 D5】

### 3.6 日报 Word 生成

**标题**：`省媒介伊蚊传染病疫情蚊媒监测情况（MM月DD日 20：00）`（**全角冒号**；MM/DD 两位补零）。

**一、布雷图指数（BI）调查评估。**

概述段落（模板，修订“共{总数}个地市”矛盾【决策：总数仅作分母，地市数单独统计】）：

> {M}月{D}日，对{地市列表}共{地市数}个地市{区县数}个县区{镇街数}个镇街{监测点数}个村居/监测点开展了蚊媒密度应急监测。监测结果显示：{达标数}个监测点BI达标准要求，达标率为{达标率}%（{达标数}/{总数}）；{风险数}个监测点BI为低中高风险，占{风险占比}%（{风险数}/{总数}）。

- **地市列表**：最终 BI 表中出现的地市，按固定顺序（广州、深圳、珠海、汕头、佛山、韶关、河源、梅州、惠州、汕尾、东莞、中山、江门、阳江、湛江、茂名、肇庆、清远、潮州、揭阳、云浮）排列，名称不加“市”，顿号分隔，最后一项用“和”连接且**保留“市”**（例：`广州、深圳、佛山和肇庆市`）。某地市被排除字段部分排除时，其后加括号注记（例：`广州（荔湾区除外）`）。
- **地市数**：出现的地市个数。
- **区县数**：不重复 (地市, 区县) 组合数；**镇街数**：不重复 (地市, 街道) 组合数。【决策 D10】
- **总数 / 监测点数**：最终 BI 表记录数。
- **达标数** = BI < 5 的记录数；**风险数** = BI ≥ 5 的记录数。
- **达标率 / 风险占比** = 计数 ÷ 总数 × 100，**保留 1 位小数**【决策 D11】，并附 `（{达标数}/{总数}）`。

风险列表段落（**高 → 中 → 低** 顺序；某等级数量为 0 则**不输出该段落**【决策 D9】）：

> {风险等级}{数量}个：{元素1}、{元素2}……

- 元素格式：`{地市+市}{区县}{街道}{监测地点}（{BI值}）`，例：`广州市天河区石牌街道塘西村（核心区）（16.2）`。
- 区县为 `市辖区`（东莞/中山）时删除区县段位：`东莞市市辖区黄江镇` → `东莞市黄江镇…`。
- 组内排序：监测指标值**从高到低**。

**二、成蚊密度（ADI）快速评估。**

结构与“一”完全相同，数据源为最终 ADI 表，文字中 BI → ADI；达标 = ADI ≤ 2，风险 = ADI > 2，风险细分按 3.5 ADI 分级。【决策 D5/D6 之 ADI 达标口径】

### 3.7 监测点汇总 Excel

- **Sheet1「BI表」**：最终 BI 表数据。**Sheet2「ADI表」**：最终 ADI 表数据。
- 列 = 地市、区县、街道、监测地点、BI*（ADI 表为 ADI*）、风险水平*（文字 + 背景色按 3.5）。
  - BI*/ADI* = 监测指标值（1 位小数）。
  - 地市/区县/街道按 3.2「监测点汇总表显示」口径（`天河区`→`天河`，`市辖区`→`/`）。
- **排序规则**：地市按固定顺序 → 区县升序 → 街道升序 → 监测地点升序 → 防控区类型升序（防控区类型仅内部保留用于排序，**输出不含该列**）【决策 D16，解决原需求自述矛盾】。中文字符串升序按 Unicode 码点比较即可。
- **Sheet3「删除数据情况说明」**：见 3.9。

### 3.8 最小区分地址算法（可执行版）

适用：3.3 B8 / 3.4 A3。对每一组（地市-区/县/市-街道/乡/镇, 社区/村居, 防控区类型）相同但地址字段有差异的记录：

1. 取 `监测地址（地图定位版）`，为空则取 `监测地址（手填）`；两者皆空 → 该记录不改监测地点；若组内仍有无法区分的多条，回退方案见第 6 步。
2. 清理地址：循环删除其中与 `地市-区/县/市-街道/乡/镇` 完全相同的连续子串、与 `社区/村居` 相同的连续子串（先删长者）。
3. 按分隔符（中文逗号`，`、英文逗号`,`、顿号`、`、空格、句号`。`）切分，取第一个非空片段为候选；若候选以 `{地市名}市` 开头，去掉该前缀（与需求示例一致）。
4. 若组内所有候选互不相同 → 候选即“最小区分地址”。
5. 若存在相同候选 → 逐步追加下一片段（候选1+候选2…），直至组内全部互异。
6. 片段用尽仍无法区分 → 保持原监测地点 `{社区/村居}（{防控区类型}）`，并对该组内冲突记录依次追加 `（1）`、`（2）`… 序号。
7. 监测地点 = `{社区/村居}{最小区分地址}（{防控区类型}）`。

**真实示例**（源数据：广州市番禺区东环街道 / 东环社区 / 警戒区，三条不同地址记录）：
- 地址1 `信业·悦都汇，GuangdongGuangzhouPanyu番禺大道北822号` → 候选 `信业·悦都汇`
- 地址2 `理想汽车广州番禺服务中心(龙美店)，…` → 候选 `理想汽车广州番禺服务中心(龙美店)`
- 地址3 `番禺电力科技园，广东省广州市番禺区东星路与莲花大道交叉口东南100米` → 候选 `番禺电力科技园`
- 结果：`东环社区信业·悦都汇（警戒区）`、`东环社区理想汽车广州番禺服务中心(龙美店)（警戒区）`、`东环社区番禺电力科技园（警戒区）`。

**需求原示例验证**：A 村两条记录，地址1 `F市B广场，C大街123号`、地址2 `A村D街道111号` → 地址2 删社区名“A村”得 `D街道111号`；地址1 候选 `F市B广场` → 去掉“F市”得 `B广场`。结果：`A村B广场（核心区）`、`A村D街道111号（核心区）`。与原需求示例完全一致。

### 3.9 删除数据情况说明（Sheet3）

- 列 = **源文件全部 32 列 + 新增列 `删除原因`**。
- 范围与原因清单（每行按处理顺序**取首个命中**的原因记录【决策 D17】）：

| 删除原因 | 对应步骤 |
|---|---|
| 监测时间为空 | P2 |
| 监测指标值为空 | P3 |
| 被排除字段过滤 | P5 |
| 距末例天数不在范围内 | P6 |
| 防控区类型不符合 | P7 |
| 行政区划无法解析 | P9 |
| 监测方法不属于BI/SSI/ADI（不参与计算） | B1/A1 筛除的 MOI、RI、BGs-Trap、双层叠帐法、诱蚊灯法、人诱 等行【决策 D3】 |
| 重复数据保留最大值 | B7 / A2 被删除的非最大值行 |

- **不列入**：监测时间不等于目标日期的行（实测单日之外约占全表 99%，列入无审计价值）【决策 D2】。

### 3.10 计算过程 Excel 的 Sheet 组成（共 9 个）

| 序号 | Sheet 名 | 来源 | 标记 |
|---|---|---|---|
| 1 | SSI表 | B3 | 无 |
| 2 | BI+SSI(不重复) | B5 | SSI 来源行标黄 |
| 3 | BI+SSI(重复)+取较大值处理 | B6 | 无（含 3 个追溯列） |
| 4 | 重复数据删除(BI) | B7 | 被删行标红 |
| 5 | 地址区分处理(BI) | B8 | 被修改行标黄 |
| 6 | 最终表(BI) | B9 | 无 |
| 7 | 重复数据删除(ADI) | A2 | 被删行标红 |
| 8 | 地址区分处理(ADI) | A3 | 被修改行标黄 |
| 9 | 最终表(ADI) | A4 | 无 |

## 4. 输出文件与命名规范

| 文件 | 命名 | 备注 |
|---|---|---|
| 计算过程 Excel | `BI_ADI_计算过程_MM月DD日.xlsx` | MM/DD 两位补零 |
| 日报 Word | `省媒介伊蚊传染病疫情蚊媒监测情况（MM月DD日 20：00）.docx` | **全角冒号**（半角 `:` 在 Windows 文件名非法） |
| 监测点汇总 Excel | `全省媒介伊蚊传染病疫点重点镇（街道）蚊媒密度监测村居一览表（MM月DD日 20：00）.xlsx` | 全角冒号 |

- 三份文件全部输出到用户选择的输出目录。
- 程序内使用文件**完整路径**读写（不经 shell），兼容含空格、中文、括号的路径（如本例输入文件名本身含空格和括号）。

## 5. GUI 要求（Tkinter）

- 输入文件：浏览按钮 + 只读文本框。
- 目标日期：年 / 月 / 日 三个下拉框（校验：月份 1–12；日按月份及闰年校验）。
- 排除字段：可选文本框（提示文案“如：荔湾区”）。
- 输出目录：选择按钮 + 文本框。
- 「开始处理」按钮（处理期间禁用，防重复点击）。
- 日志文本框：逐行追加当前步骤（“正在读取Excel…”→“正在筛选日期…”→“正在生成Word…”→“完成”）。
- 后台线程执行处理，避免界面卡死（大表 19956 行）。
- 异常处理：文件不存在、非 xlsx、列缺失（列出缺失列名）、日期非法、日期无数据 → `messagebox` 报错后**停止本次处理但不退出程序**【决策 D13】。

## 6. 异常与边界情况

- 输入日期无数据 → 弹窗“该日期无监测数据”，停止本次处理。
- 某步骤结果为空 → 对应 Sheet 只写表头。
- 数值精度：全程浮点计算，输出前统一四舍五入保留 1 位小数；**用 Decimal(ROUND_HALF_UP) 实现**（Python 内置 round 是银行家舍入，如 round(0.5)=0，不满足“四舍五入”）。
- 日期解析：GUI 下拉框直接得年月日；如后续增加文本输入，支持 `yyyy/mm/dd` 与 `yyyy-mm-dd` 两种格式。
- 键值 K 比较时空值统一视为空字符串。
- 距末例天数允许负值/0（≤5 保留）。

## 7. 技术实现与打包

- **pandas**（读 Excel、数据处理）、**openpyxl**（写带格式 Excel：单元格填充色、列宽、Sheet 顺序）、**python-docx**（生成 Word）、**Tkinter**（GUI，标准库内置）。
- **PyInstaller** 打包单文件 exe；PyInstaller 不支持跨平台编译，**必须在 Windows 上构建** → GitHub Actions 用 `windows-latest` runner（现有 `.github/workflows/build.yml` 已就绪，需补充 `requirements.txt` 与 `MosquitoMonitorTool.spec`）。
- **Win7 兼容**：Python 3.9 起官方不再支持 Win7。若需 Win7+，构建用 **Python 3.8** 并锁定兼容依赖（pandas 2.0.x、openpyxl 3.1.x、python-docx 1.x）；若仅 Win10+，可用 3.9/3.11。【决策 D15】
- 输出文件名用全角冒号，规避 Windows 非法字符。

## 8. 待用户确认的决策点清单

| 编号 | 决策 | 说明 |
|---|---|---|
| D1 | 行政区划解析按真实格式（无 `-`）重写 | 必须改，否则无法解析任何行 |
| D2 | Sheet3 不列入“监测时间不等于目标日期”的行 | 否则删除说明膨胀至上万行 |
| D3 | 非 BI/SSI/ADI 方法（MOI/RI/BGs-Trap/双层叠帐/诱蚊灯/人诱）记入 Sheet3，原因“监测方法不属于BI/SSI/ADI” | 或选择完全忽略 |
| D4 | “核心区和警戒区综合”等非核心/警戒区类型按不符合删除 | 如需保留请说明 |
| D5 | ADI 风险分级用 ADI 专属标准（≤2/2-5/5-10/>10），BI 用 <5/5-10/10-20/≥20 | 修订两套分级冲突 |
| D6 | 去重 Sheet（B7/A2）内容 = 全部行，被删行标红 | 修订“只含被删行/全部行”歧义 |
| D7 | 地址区分 Sheet（B8/A3）内容 = 全部行，被修改行标黄，不加备注列 | 修订输出范围歧义 |
| D8 | 20.5 配对：SSI↔多条BI 取 BI 最大值行配对；多条SSI↔BI 取转换后最大值 | 补充原需求未定义的配对规则 |
| D9 | Word 风险等级数量为 0 时不输出该段 | 采纳原需求“建议”选项 |
| D10 | 区县数/镇街数按 (地市,区县)、(地市,街道) 组合去重 | 跨地市同名区县算两个 |
| D11 | 达标率/风险占比保留 1 位小数 | — |
| D12 | 键值 K 中空地址字段视为空字符串 | 影响“完全一致”判定 |
| D13 | 日期无数据 → 弹窗提示并停止本次处理，不关闭程序 | — |
| D14 | Sheet 名加 (BI)/(ADI) 后缀，避免两个“最终表”同名冲突 | openpyxl 不允许同名 Sheet |
| D15 | Win7 兼容需 Python 3.8 构建；否则接受 Win10+ | 影响 build.yml 版本 |
| D16 | 监测点表“防控区类型”仅内部参与排序，输出不含该列 | 修订原需求自述矛盾 |
| D17 | Sheet3 删除原因按处理顺序取首个命中 | 一行多因时只记一条 |
| D18 | 端州区特例一般化为“街道以区县名开头则去掉该前缀” | 覆盖实测 2191 行 |
